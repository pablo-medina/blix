// Import styles
import './styles.css';

// Import game logic
import './game.js';

// Initialize game when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    console.log('Game initialized for web version');
}); 